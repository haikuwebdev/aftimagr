<%% thumbnails.each do |thumbnail| %>
  <%%= link_to_remote image_tag(thumbnail.public_filename), :url => <%= singular_name %>_path(thumbnail.parent), :method => :get %>
<%% end %>

<%%= "There are no images for this view." unless thumbnails.any? %>
